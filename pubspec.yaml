name: dart_weather_mcp
description: Weather MCP Server implementation in Dart using OpenWeather API
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  mcp_dart: ^0.5.1
  http: ^1.1.0
  args: ^2.4.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.21.0 